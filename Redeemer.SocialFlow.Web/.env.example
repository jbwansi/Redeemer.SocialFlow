# Vite development proxy target. Start the API on this port.
API_PROXY_TARGET=https://localhost:65474
# Optional production API prefix; defaults to /api. Prefer a same-origin reverse proxy.
VITE_API_BASE_URL=/api
